class Item {
  final int id;
  String name;
  String description;
  String thumbnailUrl;
  String category;
  double price;

  Item(this.id, this.name, this.description, this.thumbnailUrl, this.category,
      this.price);
}
